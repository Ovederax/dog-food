import styled from '@emotion/styled';
import { colors } from '../../theme/colors';

export const Line = styled.img`
	background: ${colors.text.form};
	display: block;
	min-width: 100%;
	height: 1px;
`;
